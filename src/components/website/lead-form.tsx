"use client";import{FormEvent,useState}from"react";export function LeadForm({kind="consultation"}:{kind?:string}){const[done,setDone]=useState(false);return <form className="public-lead-form" onSubmit={(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setDone(true)}}><label>Full name<input required placeholder="Enter your name"/></label><label>Mobile number<input required inputMode="tel" pattern="[0-9+ ]{10,15}" placeholder="Enter mobile number"/></label><label>Email address<input type="email" placeholder="Enter email address"/></label><label>Message<textarea placeholder={`Tell us about your ${kind} requirement`}/></label><button className="site-gradient">Submit Request</button>{done&&<p>Thank you. Our team will contact you shortly.</p>}</form>}
