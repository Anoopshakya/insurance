"use client";import Image from"next/image";import{FormEvent,useEffect,useState}from"react";import{onAuthStateChanged}from"firebase/auth";import{auth}from"@/lib/firebase-client";
export default function PartnerProfile(){const[error,setError]=useState("");const[loading,setLoading]=useState(false);useEffect(()=>onAuthStateChanged(auth,user=>{if(!user)location.replace("/partner/register")}),[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setLoading(true);const user=auth.currentUser;if(!user)return;const form=new FormData(e.currentTarget);const token=await user.getIdToken(true);const response=await fetch("/api/partner/onboarding/profile",{method:"POST",headers:{Authorization:`Bearer ${token}`},body:form});if(!response.ok){setError((await response.json()).error);setLoading(false);return}await user.getIdToken(true);location.replace("/partner")}return <main className="partner-login-page"><section className="partner-login-card profile-card"><Image src="/brand/magikpolicy-logo.png" alt="MagikPolicy" width={420} height={140}/><p className="partner-kicker">Partner application</p><h1>Complete your profile</h1><p>Provide settlement bank information and one government identity document for verification.</p><form onSubmit={submit}><label>Account holder name<input name="accountHolder" required/></label><label>Bank name<input name="bankName" required/></label><label>Account number<input name="accountNumber" inputMode="numeric" minLength={6} required/></label><label>IFSC code<input name="ifsc" required/></label><label>Identity type<select name="idType" required defaultValue=""><option value="" disabled>Select ID</option><option value="pan">PAN card</option><option value="aadhaar">Aadhaar card</option><option value="passport">Passport</option><option value="driving_licence">Driving licence</option></select></label><label>Identity document<input name="document" type="file" accept="image/jpeg,image/png,application/pdf" required/><small>PDF, JPG or PNG · Maximum 5 MB</small></label>{error&&<p className="partner-error">{error}</p>}<button disabled={loading}>{loading?"Submitting securely…":"Submit for approval"}</button></form></section></main>}
